# AWS Production Environment Variables
# Use this template for AWS deployments
# Copy to .env and update with actual values

# Media files path - use EFS mount or EC2 instance path
# Example: MEDIA_HOST_PATH=/mnt/efs/microbiome-media
# Or: MEDIA_HOST_PATH=/opt/microbiome-ai/media
MEDIA_HOST_PATH=/mnt/efs/microbiome-media

# Nextflow cache - EFS for multi-instance or local for single EC2
# EFS (shared, persistent): NEXTFLOW_CACHE_PATH=/mnt/efs/nextflow-cache
# Local EC2 (faster, single instance): NEXTFLOW_CACHE_PATH=/opt/nextflow-cache
NEXTFLOW_CACHE_PATH=/mnt/efs/nextflow-cache

# Conda environments - EFS for sharing or local for performance
# EFS (shared): CONDA_ENVS_PATH=/mnt/efs/conda-envs
# Local (faster): CONDA_ENVS_PATH=/opt/conda-envs
# Or use named volume (default): # CONDA_ENVS_PATH=conda-envs
CONDA_ENVS_PATH=/mnt/efs/conda-envs

# Django settings
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY
DEBUG=False
ALLOWED_HOSTS=your-domain.com,your-ec2-ip

# Database (optional - defaults to SQLite)
# DATABASE_URL=postgresql://user:password@rds-endpoint:5432/dbname

# Nextflow Java options (adjust based on EC2 instance size)
NXF_OPTS=-Xms1G -Xmx4G

