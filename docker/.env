# Local development environment variables
MEDIA_HOST_PATH=/home/katwre/projects/Microbiome-ai-dev/backend/microbiome-backend/media
SECRET_KEY=dev-secret-key-change-in-production
DEBUG=False

# Optional: Use named Docker volumes (default) or bind mounts for persistence
# NEXTFLOW_CACHE_PATH=nextflow-assets  # Named volume (default)
# CONDA_ENVS_PATH=conda-envs  # Named volume (default)
