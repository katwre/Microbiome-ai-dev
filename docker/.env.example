# Environment variables for Docker Compose
# Copy this to .env and adjust for your environment

# Media files path (absolute path for uploaded files and analysis results)
# For local development, use your actual path
# For AWS/production, use /mnt/efs/media or similar
MEDIA_HOST_PATH=/home/yourusername/projects/Microbiome-ai-dev/backend/microbiome-backend/media

# Django settings
SECRET_KEY=your-secret-key-change-in-production
DEBUG=False

# Optional: Override default Docker volume names with bind mount paths
# Leave commented to use named Docker volumes (recommended for local dev)
# NEXTFLOW_CACHE_PATH=/path/to/nextflow-cache
# CONDA_ENVS_PATH=/path/to/conda-envs
